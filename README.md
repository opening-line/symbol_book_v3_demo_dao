# symbol_book_v3_demo_dao

次世代組織のための実用 Symbol ブロックチェーンアプリケーションのデモアプリ（DAO）

## 注意事項

push 前に以下を実行することを心がけたい

```
$ docker compose run --rm client npm run lint
$ docker compose run --rm server npm run lint
```
